import ExemplosArrayList.ExemploCarro;

public class ExemploForArrayList {
    public static void main(String[] args) {
        // Instanciar um objeto é criar uma objeto com seus atributos(caracteristicas) e métodos(ações)
        // Cachorro => (atributos) quantidade pata, raça, idade, mudo, quantidade latidas por dia
        // Cachorro => (metodos) latir(), correr(), andar(), dormir()

        // Conta    => (atributos) saldo, cliente, cpf, historico, limite
        // Conta    => (metodos) pagar(), receber(), imprimirExtrato()
        
        // Instanciar (pensar na palavra new) um objeto da classe ExemploCarro
        ExemploCarro executor = new ExemploCarro();
        executor.executar();

    }
}
